// Chip is the same shape as Button by design — a pill-style label
// rendered as a button so it gets focus + keyboard activation for free.
export { Button as Chip, type ButtonProps as ChipProps } from './Button'
