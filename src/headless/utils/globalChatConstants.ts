/** `ChatContextType` of the app-level assistant chat. */
export const GLOBAL_CHAT_CONTEXT_TYPE = 'GENERAL'

/** Title the global assistant surface carries in every client. */
export const GLOBAL_CHAT_TITLE = 'Assistant'

/** Accessible name of the control that retires the conversation and starts another. */
export const GLOBAL_CHAT_RESET_LABEL = 'New conversation'
