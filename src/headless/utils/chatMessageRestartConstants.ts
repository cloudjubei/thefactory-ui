/** Restart control on the chat's trailing user message — the agent runs again on it. */
export const MESSAGE_RESTART_LABEL = 'Run the agent again on this message'

/** Same control while a turn streams: it refuses, and says why. */
export const MESSAGE_RESTART_BUSY_LABEL = 'Wait for the turn to finish before running it again'

/** Short visible caption next to the icon. */
export const MESSAGE_RESTART_ACTION_LABEL = 'Try again'
